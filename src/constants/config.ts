export const Config = {
  defaultTaskDuration: 30, // minutes
  maxSubtasks: 20,
  defaultNotificationOffset: 15, // minutes before
  dateFormat: 'yyyy-MM-dd',
  timeFormat: 'HH:mm',
  dbName: 'daydeck.db',
} as const;
